/**
 * Created by Chinthaka on 4/17/15.
 */

(function() {
  'use strict';
  angular.module('sl-lottery-results.main', [])

    .controller('MainCtrl', [
      '$scope',
      '$ionicNavBarDelegate',
      function($scope,
               $ionicNavBarDelegate) {
        function initialization() {
        }

        initialization();
      }
    ]);
})();

